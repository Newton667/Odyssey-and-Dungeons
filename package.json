{
  "name": "ond-odyssey-and-dragons",
  "version": "0.2.0",
  "description": "OND - Your local D&D companion app",
  "author": "Newton667",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"cd server && node server.js\" \"cd client && npx vite\"",
    "build:client": "cd client && npx vite build",
    "deps": "node scripts/ensure-deps.js",
    "postinstall": "node scripts/ensure-deps.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
